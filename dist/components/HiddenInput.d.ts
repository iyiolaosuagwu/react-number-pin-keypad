import React from 'react';
interface HiddenInputProps {
    length: number;
    maxLength: number;
    className?: string;
    dotClassName?: string;
}
export declare function HiddenInput({ length, maxLength, className, dotClassName, }: HiddenInputProps): React.JSX.Element;
export {};
